#include<stdio.h>
void main()
{
      int i,n,sum;
      printf("\nenter the value of n :");
      scanf("%d",&n);
      for(i=1;i<=n;i++)
      {
            sum+=i;
      }
      printf("\nsum=%d",sum);
}
